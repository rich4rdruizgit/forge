---
name: forge-close
description: >
  Archive a completed Forge feature cycle. Validates all required phases are approved (SPIKE optional, SPEC/BUILD/VERIFY required),
  generates INDEX.md as-built reference, extracts knowledge — writes full content to forge-memory (canonical) and one-liner
  index entries to KNOWLEDGE.md (thin index, max 80 lines) — moves artifacts to closed/,
  updates FORGE.md history, and resets active cycle state.
  Trigger: `forge close` command with all required phases approved.
license: Apache-2.0
metadata:
  author: doubler
  version: "3.0"
---

## Purpose

Closes a completed feature cycle: validates all required phases are `✅ Aprobado` (SPIKE is optional — if skipped, it does not need approval), generates INDEX.md from all approved artifacts, moves all artifacts (including INDEX.md) from `.forge/features/activo/{slug}/` to `.forge/features/closed/{slug}/`, adds a history row to FORGE.md, and resets the active cycle to null.

---

## Preconditions

- Active feature exists (`feature` ≠ null in FORGE.md)
- ALL required phases = `✅ Aprobado`: SPEC, BUILD, VERIFY must be approved. SPIKE is optional — if it was used (status is not `⏳ Sin iniciar`), it must also be `✅ Aprobado`. If SPIKE was skipped (`⏳ Sin iniciar`), it does not block closing.
- If ANY required phase is not approved: STOP, list unapproved phases

---

## Forge Runtime

→ Execute `_shared/forge-runtime.md` steps R0–R4 before any skill-specific logic.
→ **R5 exception**: K5 handles `forge_mem_session_summary` and `forge_mem_session_end` explicitly. Do NOT run R5 after skill logic — session close is already managed within Step C2.5.

---

## Execution Steps

ORDER IS CRITICAL — do not reorder these steps.

### Step C1 — Verify activo folder exists
Confirm `.forge/features/activo/{slug}/` exists and contains files.
If not found: STOP. Error E601: "No encontré `.forge/features/activo/{slug}/`. ¿Fue movida manualmente?"

### Step C2 — Generate INDEX.md

Read ALL approved artifacts from `.forge/features/activo/{slug}/`:
- SPIKE.md (if exists — optional)
- SPEC.md
- VERIFY.md
- TRACEABILITY.md

Also read if they exist (generated by forge approve):
- VALIDATION-SPIKE.md
- VALIDATION-SPEC.md
- VALIDATION-BUILD.md
- VALIDATION-VERIFY.md

Generate INDEX.md in `.forge/features/activo/{slug}/INDEX.md` following the INDEX.md template.

Populate these sections:
- **Acceptance Criteria**: Extract all ACs from SPEC.md §1 Requirements
- **Domain Events**: Extract all events from SPEC.md §2 Domain Model
- **UI Contract Summary**: Extract UI states/screens from SPEC.md §5 UI Contract
- **Architecture Decisions**: Extract all decisions from SPEC.md §4 Decisions
- **Key Files**: Extract file list from SPEC.md §3 Architecture
- **Test Coverage Metrics**: Extract from VERIFY.md (total tests, coverage %)
- **Validation Scores**: Extract final scores from each VALIDATION-*.md file (if present)
- **Traceability Summary**: Extract AC→Event→UI State→Test→Implementation counts from TRACEABILITY.md (if present)
- **Patterns & Conventions**: Notable patterns used (from SPEC.md §3 Architecture)
- **Addenda**: Extract any addenda from SPEC.md Addenda section (if present)
- **Tags**: Auto-generate searchable tags from stack, feature name, patterns used

### Step C2.5 — Knowledge Extraction

After generating INDEX.md and BEFORE archiving, extract and persist knowledge.

**IMPORTANTE**: El orden es K1 → K2 → K3 → K5 → K4.
forge-memory es la fuente canónica. KNOWLEDGE.md es el índice delgado.

#### Step K1 — Read artifacts
Read: approved SPEC, VERIFY report, TRACEABILITY.md, INDEX.md

#### Step K2 — Extract candidates
For each knowledge category, identify candidates:
- **Patrones**: ¿Se usó o estableció un patrón reutilizable?
- **Decisiones Técnicas**: ¿Alguna decisión afecta más que esta feature?
- **Contratos / Componentes**: ¿Nuevo endpoint, interfaz, o componente reutilizable?
- **Errores y Lecciones**: ¿Algo inesperado que un dev futuro debería saber?
- **Módulos Tocados**: SIEMPRE registrar

#### Step K3 — Present to dev
Present each candidate with concrete format. The dev approves, edits, or rejects EACH ONE individually.
**Rule**: NUNCA persistir una entrada sin aprobación explícita del dev por entrada.

#### Step K5 — Write full content to forge-memory (CANONICAL STORE)

**Este paso va ANTES de K4.** forge-memory recibe el contenido completo.

**If `forge_memory_available`:**

1. Por cada entrada aprobada en K3, call `forge_mem_save`:
   ```
   title: "{entry title}"
   type: "knowledge"
   topic_key: "knowledge/{category}/{slug-del-entry}"
     — donde category es: pattern | decision | contract | component | error
     — ejemplos: "knowledge/pattern/base-viewmodel"
                 "knowledge/decision/DT-005"
                 "knowledge/error/ERR-002"
   content:
     **Feature**: {slug}
     **Date**: {today}
     **Category**: {category}
     {contenido completo aprobado en K3 — sin truncar}
   ```
   Guardar el `topic_key` retornado por cada llamada para usarlo en K4.

2. Call `forge_mem_knowledge_extract` con:
   ```
   feature: {slug}
   entries: {approved entries from K3}
   ```

3. Save feature summary — call `forge_mem_save` with:
   ```
   title: "Feature cerrada: {feature}"
   type: "project"
   topic_key: "forge/{slug}/summary"
   content:
     feature: {feature}
     slug: {slug}
     stack: {stack from config.yaml}
     fecha_cierre: {today}
     validation_score: {overall score or N/A}
     acceptance_criteria: {list all ACs from SPEC.md}
     domain_events: {list all events from SPEC.md Domain Model}
     architecture_decisions: {list all decisions from SPEC.md Decisiones Técnicas}
     key_files: {list from SPEC.md Components}
     patterns_used: {patterns from approved K3 entries}
     tags: {auto-generated tags from INDEX.md}
   ```

4. Call `forge_mem_session_summary` con:
   ```
   goal: "Feature cerrada: {title}"
   accomplished:
     - {N} ACs implementados
     - Quality Score SPEC: {X}/10
     - Patterns applied: {list from K3}
   discoveries:
     - {entries from "Errores y Lecciones" approved in K3}
   next_steps: []
   relevant_files:
     - .forge/features/closed/{slug}/SPEC.md
     - .forge/features/closed/{slug}/TRACEABILITY.md
     - .forge/features/closed/{slug}/VERIFY.md
   ```

5. Call `forge_mem_session_end(project: {slug})`

**If forge-memory NOT available:** skip K5 silently, continuar con K4. En este caso KNOWLEDGE.md recibirá el contenido completo como fallback de último recurso (ver regla K4-fallback más abajo).

#### Step K4 — Update KNOWLEDGE.md index (THIN INDEX)

KNOWLEDGE.md es un índice delgado de máx 80 líneas. NO almacena contenido completo.

**Límite duro**: Antes de escribir, contar las líneas del archivo. Si agregar las nuevas entradas supera las 80 líneas, eliminar las filas más antiguas de cada tabla (mantener las últimas N) hasta caber dentro del límite.

**Si forge-memory estuvo disponible en K5** — escribir ONE-LINER por entrada aprobada:

- **Module Hotspots**: agregar fila `| {module} | {slug} | {date} | {1-line note} |`
  Mantener últimas 10 filas. Eliminar las más antiguas si se supera.

- **Recent Decisions**: agregar fila `| {DT-N} | {1-line title} | {slug} | {topic_key de K5} |`
  Mantener últimas 5 filas.

- **Active Patterns**: agregar fila `| {pattern name} | {slug} | {topic_key de K5} |`
  Sin límite de filas fijo, pero respetar el límite global de 80 líneas.

- **Contracts & Components**: agregar fila `| {name} | {type} | {module} | {topic_key de K5} |`

- **Recent Errors & Lessons**: agregar fila `| {ERR-N} | {1-line title} | {slug} | {topic_key de K5} |`
  Mantener últimas 3 filas.

Actualizar **Stats** y **Last updated** en el encabezado.

**K4-fallback (forge-memory NO disponible en K5)**: Escribir contenido completo de cada entrada aprobada directamente en KNOWLEDGE.md (comportamiento legacy). Advertir al dev:
```
⚠️ forge-memory no disponible — contenido completo guardado en KNOWLEDGE.md.
   Activá forge-memory para evitar que este archivo crezca sin control.
```

**Rule**: NUNCA escribir a KNOWLEDGE.md sin aprobación explícita del dev (viene de K3).

### Step C3 — Create closed folder if needed
Check if `.forge/features/closed/` exists. If not: create it. Not an error.

### Step C4 — Handle slug collision
Check if `.forge/features/closed/{slug}/` already exists.
If exists: use `{slug}-{YYYY-MM-DD}` as target folder name (append today's date).

### Step C5 — Move all artifacts
Move all files from `.forge/features/activo/{slug}/` to `.forge/features/closed/{slug}/` (or slug with date suffix).
This includes SPIKE.md (if exists), SPEC.md, VERIFY.md, TRACEABILITY.md, INDEX.md, and any VALIDATION-*.md files.
This is a MOVE, not a copy. The activo/{slug}/ folder must be empty afterward.

### Step C6 — Remove activo slug folder
Remove the now-empty `.forge/features/activo/{slug}/` directory.

### Step C7 — Update FORGE.md history table (DO THIS BEFORE RESETTING ACTIVE STATE)
Add a row to the history table:
| {feature} | {slug} | {azure_story or —} | {YYYY-MM-DD} | ✅ Cerrada | {overall validation score or —} |

The overall validation score is the average of all phase validation scores, or `—` if no validation was run.

### Step C8 — Reset FORGE.md active cycle
Reset the YAML active cycle block:
  feature:       null
  slug:          null
  azure_story:   null
  fase_actual:   null

Reset the phase table — all 4 rows:
  | Fase | Archivo | Estado | Validation Score |
  | SPIKE  | —       | ⏳ Sin iniciar | — |
  | SPEC   | —       | ⏳ Sin iniciar | — |
  | BUILD  | —       | ⏳ Sin iniciar | — |
  | VERIFY | —       | ⏳ Sin iniciar | — |

---

## Success Output

```
✅ Feature `{feature}` cerrada exitosamente.

📁 Artefactos archivados en: .forge/features/closed/{slug}/
📄 INDEX.md generado con as-built reference
📋 FORGE.md reseteado — listo para la próxima feature.
📜 Historial actualizado.

Para comenzar una nueva feature: `forge new "nombre de la nueva feature"`
```

---

## Rules

- **NUNCA cerrar si cualquier fase no está ✅ Aprobado — sin excepciones**
- SIEMPRE generar INDEX.md (Step C2) ANTES de mover artefactos (Step C5)
- SIEMPRE actualizar el historial (Step C7) ANTES de resetear el estado activo (Step C8)
- SIEMPRE mover los artefactos (no copiar) — la carpeta activo/{slug}/ debe quedar vacía
- NUNCA borrar artefactos — van a closed/, no a la papelera
- NUNCA resetear FORGE.md si la operación de mover falló

---

## Return Envelope

### Success
```
**Estado**: `complete`
**Resumen**: Feature `{feature}` cerrada. INDEX.md generado. Artefactos movidos a `.forge/features/closed/{slug}/`. FORGE.md reseteado.
**Artefacto**: .forge/features/closed/{slug}/
**Siguiente comando**: `forge new "nombre feature"`
```

### Blocked
```
**Estado**: `blocked`
**Error**: {EXXX — message}
**Acción requerida**: {what to do}
```
